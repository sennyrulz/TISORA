{
  "name": "tisora-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontEnd && npm install && npm run build"
  },
  "workspaces": [
    "frontEnd",
    "backEnd",
    "admin"
  ]
}
